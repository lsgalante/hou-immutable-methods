// Template greyrange
PSI_TOKEN.gad = 
{
   LAYOUT(vertical) HSTRETCH SPACING(0.01)

#if PSI_USE_LABEL
   PSI_TOKEN.label = PARM_LABEL("PSI_LABEL", "PSI_TOKEN") PSI_LABEL_SIZE;
   PARM(PSI_TYPE, PSI_SIZE, "PSI_TOKEN", none, none) HSTRETCH
	LABELREF(PSI_TOKEN.label);
#else
   PARM(PSI_TYPE, PSI_SIZE, "PSI_TOKEN", none, none) HSTRETCH;
#endif

#if PSI_STANDARD_PARMS
   // Do not display in the toolbox.
   RANGESLIDER PSI_MIN PSI_MAX 2 LOOK(greyramp) VALUE(PSI_TOKEN.val)
	HEIGHT(0.25) HSTRETCH;
#endif

#include "config/Templates/ParamV3/psi_script_action.ui"
}


